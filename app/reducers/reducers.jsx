export const dungeonMapReducer = (state = {}, action) => {

	switch (action.type) {
		default:
			return state;
	}
}

export const characterReducer = (state = {}, action) => {

	switch (action.type) {
		default:
			return state;
	}
}